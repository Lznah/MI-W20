{
  "name": "tutorial4",
  "version": "1.0.0",
  "description": "Implement a simple Node.js application enabling login (generates and returns JWT token) and access to a resource with the valid token.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "crypto": "^1.0.1"
  }
}
